# Recebe a palavra que o usuário digitar
word = input("Digite o primeiro dado: ")
# Recebe o número que o usuário digitar e converte para inteiro
number = int(input("Digite o segundo dado: "))

# Repete a palavra o número de vezes especificado pelo usuário
resultado = (word + " ") * number

# Printa o resultado
print(resultado)
